diff -Nur anjuta-0.1.9/data/Makefile.am anjuta-0.1.9.new/data/Makefile.am
--- anjuta-0.1.9/data/Makefile.am	Thu Feb 14 20:28:34 2002
+++ anjuta-0.1.9.new/data/Makefile.am	Sun Apr 28 04:55:33 2002
@@ -31,13 +31,13 @@
 MACROS2_dir = $(prefix)/@NO_PREFIX_PACKAGE_DATA_DIR@/gnome2
 
 install-data-local: system.tags
-	mkdir -p $(MACROS_dir)
-	mkdir -p $(MACROS2_dir)
-	$(INSTALL_DATA) $(MACROS:%=$(top_srcdir)/macros/%) $(MACROS_dir)
-	$(INSTALL_DATA) $(MACROS2:%=$(top_srcdir)/macros2/%) $(MACROS2_dir)
-	mv $(MACROS2_dir)/Makefile.am.project $(MACROS2_dir)/Makefile.am
-	$(INSTALL_DATA) system.tags $(anjuta_datadir)
-	$(INSTALL_DATA) $(top_srcdir)/setup-gettext $(anjuta_datadir)
+	mkdir -p $(DESTDIR)$(MACROS_dir)
+	mkdir -p $(DESTDIR)$(MACROS2_dir)
+	$(INSTALL_DATA) $(MACROS:%=$(top_srcdir)/macros/%) $(DESTDIR)$(MACROS_dir)
+	$(INSTALL_DATA) $(MACROS2:%=$(top_srcdir)/macros2/%) $(DESTDIR)$(MACROS2_dir)
+	mv $(DESTDIR)$(MACROS2_dir)/Makefile.am.project $(DESTDIR)$(MACROS2_dir)/Makefile.am
+	$(INSTALL_DATA) system.tags $(DESTDIR)$(anjuta_datadir)
+	$(INSTALL_DATA) $(top_srcdir)/setup-gettext $(DESTDIR)$(anjuta_datadir)
 
 uninstall-local:
 	@rm -fv $(anjuta_datadir)/system.tags
diff -Nur anjuta-0.1.9/manuals/sgmldocs.make anjuta-0.1.9.new/manuals/sgmldocs.make
--- anjuta-0.1.9/manuals/sgmldocs.make	Thu Feb 14 20:28:34 2002
+++ anjuta-0.1.9.new/manuals/sgmldocs.make	Sun Apr 28 04:40:50 2002
@@ -52,8 +52,6 @@
 index.html: $(docname)/index.html
 	-cp $(docname)/index.html .
 
-$(docname).sgml: $(sgml_ents)
-        ourdir=`pwd` && cd $(srcdir) && cp -f $(sgml_ents) $$ourdir || true
 
 # The weird srcdir trick is because the db2html from the Cygnus RPMs
 # cannot handle relative filenames
